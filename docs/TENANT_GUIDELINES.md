# Tenant Guidelines

- Super admin can access all tenants.
- Tenant admin can access only their tenant.
- Supervisor can access only assigned agency scope.
- Agent can access only their own tickets, sessions, balance, and sales.
- Every business table should include `tenant_id` unless the scope is inherited through a parent table and documented.
