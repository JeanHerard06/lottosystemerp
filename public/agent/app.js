const API='../../api';
function token(){return localStorage.getItem('token')||''}
async function api(path, opts={}){opts.headers=opts.headers||{}; if(token()) opts.headers.Authorization='Bearer '+token(); const r=await fetch(API+'/'+path, opts); return await r.json();}
async function login(){const f=new FormData(); f.append('username',document.querySelector('#username').value); f.append('password',document.querySelector('#password').value); const d=await api('login.php',{method:'POST',body:f}); if(d.success){localStorage.setItem('token',d.token); localStorage.setItem('user',JSON.stringify(d.user)); location.href='dashboard.html'}else alert(d.message)}
function logout(){localStorage.clear(); location.href='index.html'}
async function loadDashboard(){const d=await api('dashboard.php'); if(!d.success){logout();return;} document.querySelector('#ventes').textContent=d.data.ventes; document.querySelector('#fiches').textContent=d.data.fiches; document.querySelector('#profit').textContent=d.data.profit;}
function addLine(){document.querySelector('#plays').insertAdjacentHTML('beforeend', `<div class="grid grid-cols-3 gap-2 mb-2"><input name="number" placeholder="Numéro" class="border p-3 rounded"><select name="type" class="border p-3 rounded"><option value="borlette">Borlette</option><option value="mariage">Mariage</option><option value="lotto3">Lotto 3</option><option value="lotto4">Lotto 4</option></select><input name="amount" type="number" step="0.01" placeholder="Montant" class="border p-3 rounded"></div>`)}
async function saveFiche(){const rows=[...document.querySelectorAll('#plays > div')]; const items=rows.map(r=>({number:r.querySelector('[name=number]').value,type:r.querySelector('[name=type]').value,amount:r.querySelector('[name=amount]').value})).filter(x=>x.number&&x.amount); const d=await api('fiches_store.php',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({items})}); if(d.success){alert('Fiche '+d.data.fiche_code+' enregistrée'); location.href='dashboard.html'}else alert(d.message||'Erreur')}
if('serviceWorker' in navigator) navigator.serviceWorker.register('sw.js');
