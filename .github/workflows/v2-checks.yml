name: V2 Checks

on:
  pull_request:
    branches: [ develop/v2.0, master, main ]

jobs:
  php-syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: PHP syntax check
        run: find . -path './vendor' -prune -o -name '*.php' -print0 | xargs -0 -n1 php -l
